###
### generated by script
###

language: java
sudo: false
install: true

jdk:
- openjdk9


script:
- "./gradlew clean build"

after_success:
- bash <(curl -s https://scripts.scantist.com/staging/ci-travis-jar.sh)

cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.sonar/cache"
  - "$HOME/.gradle"
  - ".gradle"
